# VERSION_MAP — 版本差异记录（按需回填，只记本项目关心的）

> 数据源：4.3 以 `IFC4_3`（ADD2 盖章版）为准；旧版本查 `IFC4.3.x-development\reference_schemas\`（IFC2X3_TC1.exp / IFC4_ADD2_TC1.exp / IFC4x1 / IFC4x2 + psd_*）。

## IfcDoor 跨版本读取注意事项

（待回填）

## 门宽字段优先读取顺序

（待回填：OverallWidth → Qto_DoorBaseQuantities.Width → 几何提取，各版本可用性待核）

## FireRating / FireExit 位置与可用性

（待回填：IFC2x3 中 FireRating 在 Pset_DoorCommon 的定义差异待核）

## 碰撞检测常用类别的版本稳定性

（待回填：注意 IFC4.3 新增基础设施实体在旧版不存在）
